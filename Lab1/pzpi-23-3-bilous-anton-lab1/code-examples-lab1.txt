Назва інструменту та версія:
Claude Code (Anthropic), модель Claude Sonnet 4.6.

Мета використання:
Інструмент використано для аналізу вихідного коду системи LumiRum та формування
оновленого тексту документа Vision & Scope. Зокрема:

- аналіз модулів серверної частини (Rust/Axum) та прошивки IoT-клієнта (C++/Arduino);
- формування тексту розділу 3.1 Scope of Initial Release;
- формування тексту розділу 3.2 Scope of Subsequent Releases;
- формування тексту розділу 3.3 Limitations and Exclusions;
- оновлення розділу 4.3 Description of Technologies.

Внесок автора:
Автор сформулював вихідний запит, надав вихідний код системи та попередню версію
документа Vision & Scope. Після отримання результатів автор перевірив відповідність
згенерованого тексту архітектурі системи, відредагував формулювання, визначив
точний склад клієнтів (лише Android, без iOS у першому релізі), а також скомпонував
фінальний документ і звіт та записав відео.


ВИКОРИСТАНІ ЗАПИТИ
==================

1. Запит: аналіз вихідного коду та оновлення Vision & Scope

Запит до ШІ:
"Тобі необхідно:
- прочитати spec.md з повним описом мого бачення системи
- проаналізувати src код
- створити файл з контекстом
- оновити старий vision and scope файл щоб він відповідав поточному стану системи
"

Результат (витяг):

[Аналіз коду]
Проаналізовано модулі: auth (Role: Admin/Owner/User, JWT + API key), profiles (циркадні
профілі зі sleep_start/sleep_end, координатами, timezone), devices (secret_key, is_public,
firmware_version), telemetry (event_type, motion_detected, brightness, color_temp), circadian
(4-фазний алгоритм: Morning Boost → Daylight → Evening Relaxation → Wind Down),
system (stats, user management — Admin only). IoT-клієнт реалізовано мовою C++ з
фреймворком Arduino (не Rust, як планувалося): WiFi, NTP, HTTP, NeoPixel WS2812B,
PIR, потенціометр, Config Mode через веб-сервер, NVS для API-ключа.

[Розділ 3.1 — витяг]
Серверна частина (Backend):
- REST API для взаємодії з клієнтами та IoT-пристроями, задокументований через
  OpenAPI (Scalar).
- Математичний модуль розрахунку 4-фазних кривих циркадного освітлення з
  урахуванням астрономічних координат та часового поясу.
- Рольова система автентифікації та авторизації: Admin (системний адміністратор),
  Owner (власник пристроїв) та User (кінцевий користувач, підпорядкований Owner).
- JWT-автентифікація для людей-користувачів та автентифікація через API-ключ для
  IoT-пристроїв.
...

[Розділ 3.2 — витяг]
Застосування AI у системі: Предиктивна аналітика на основі накопичених даних
телеметрії дозволить системі автоматично адаптувати розклади освітлення під реальні
звички користувача.

Збір даних для навчання: Система вже збирає телеметрію подій від IoT-пристроїв: тип
події, стан руху, яскравість, колірна температура, часова мітка.

Підхід до навчання та використання моделі: Планується використання готового API
великої мовної моделі (наприклад, Google Gemini або Claude API) для генерації
персоналізованих рекомендацій. Додатково розглядається fine-tuning легкої моделі
(на базі TensorFlow Lite) для локальної класифікації патернів активності.

[Розділ 3.3 — витяг]
- Відсутність шифрування даних у спокої. Шифрування на рівні рядків навмисно
  виключено через значне ускладнення запитів і відсутність критичних персональних
  даних у телеметрії.
- Відсутність Mesh-мережі. IoT-пристрої взаємодіють виключно через WiFi-роутер.
- Локальне розгортання сервера. Хмарне керування та OTA-оновлення прошивки
  навмисно виключені для збереження автономності та приватності.
- Одна мережа WiFi на пристрій.


2. Запит: наповнення звіту лабораторної роботи

Запит до ШІ:
"Тепер прочитай мої зміни в vision and scope та онови опис виконаної роботи в файлі звіту"

Результат (витяг з розділу "Опис виконаної роботи"):
На першому етапі роботи було проаналізовано попередню версію документа Vision & Scope
та вихідний код системи: серверну частину (Rust/Axum) і прошивку IoT-клієнта
(C++/Arduino). Аналіз виявив розбіжності між задокументованим станом і фактичною
реалізацією: IoT-клієнт виконано мовою C++ з використанням фреймворку Arduino
замість Rust, а до переліку компонентів системи додано веб- та мобільний клієнти.
...
